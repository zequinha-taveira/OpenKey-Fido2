[build-system]
requires = ["maturin>=1.4,<2"]
build-backend = "maturin"

[project]
name = "openkey-python"
version = "0.1.0"
description = "Virtual Authenticator FIDO2/CTAP2 em processo via pyo3 (módulo openkey_core)"
requires-python = ">=3.9"
license = { text = "MIT" }

[tool.maturin]
module-name = "openkey_core"
